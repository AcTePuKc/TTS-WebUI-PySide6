FROM python:3.11-slim

WORKDIR /app

COPY requirements.uv.toml /app/
RUN pip install uv && \
    uv pip install --system requirements.uv.toml

COPY . /app

CMD ["python", "-m", "gui_pyside6.backend.api_server"]
