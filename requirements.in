# Core runtime deps for Hybrid TTS App (PySide6 GUI + optional FastAPI server)

# GUI
pyside6 >= 6.6

# API
fastapi >= 0.110
uvicorn >= 0.25

# TTS core
torch >= 2.2
torchaudio
soundfile

# Utils
safetensors
python-dotenv==1.0.0
pillow==10.3.0
pydantic==2.10.6
accelerate>=0.33.0

# Optional (platform specific)
# matplotlib

# Other
deprecated >= 1.2.13